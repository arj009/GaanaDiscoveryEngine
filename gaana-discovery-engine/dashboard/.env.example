# Optional: FastAPI backend URL (Render deployment from render.yaml)
# Leave empty to use bundled static data in /public/enriched_reviews.json
# After deploying to Render, replace with your actual Render URL
VITE_API_URL=https://your-gaana-discovery-api.onrender.com
